using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CollisionScript : MonoBehaviour
{
    public GameObject boardController;
    /*private bool ready = false;
     IEnumerator OnCollisionEnter(Collision collision)
    {
        // if (!boardController.GetComponent<BoardController>().fufufuffuu)
        if (!ready){
            yield return new WaitForSeconds(4.0f);
            ready = true;
        }
        if (ready)
        {
            Debug.Log("Colloison");
            boardController.GetComponent<BoardController>().ReplacePiece();
        }
        //Debug.Log(boardController.GetComponent<BoardController>().getFUFU() + " " + Time.time);
        //if (boardController.GetComponent<BoardController>().fufufuffuu)
          
        // boardController.GetComponent<BoardController>().SwitchPlayer();
    }*/
 
}
